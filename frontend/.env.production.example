# Frontend production environment — copy to .env.production.local and fill in values.
# These NEXT_PUBLIC_* vars are baked into the Next.js bundle at build time.
# They are not secret — they end up in the browser bundle.

NEXT_PUBLIC_API_URL=http://api:8000
NEXT_PUBLIC_SITE_URL=https://susdevos.com
